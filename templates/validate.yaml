{{/*
This template exists solely to trigger validation checks on every render.
It produces no Kubernetes resources — the file is empty after rendering.
*/}}
{{- include "pelican-cache.validateRequiredValues" . }}
