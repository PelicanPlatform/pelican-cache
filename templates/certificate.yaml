{{- if .Values.tls.certManager.enabled }}
{{- $dnsNames := concat (list (required "serverHostname is required" .Values.serverHostname)) (.Values.tls.certManager.dnsNames | default (list)) | uniq }}
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ include "pelican-cache.fullname" . }}-cert
  labels:
    {{- include "pelican-cache.labels" . | nindent 4 }}
spec:
  dnsNames:
    {{- toYaml $dnsNames | nindent 4 }}
  issuerRef:
    name: {{ .Values.tls.certManager.issuerRef.name }}
    kind: {{ .Values.tls.certManager.issuerRef.kind }}
  secretName: {{ include "pelican-cache.fullname" . }}-tls
{{- end }}
